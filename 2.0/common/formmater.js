
let format = (payload)=>{
  let {type,value,onError,onSuccess}= payload
  // TODO
  // TODO ADDRESS ethereumUtil.toChecksumAddress('0x' + address.toString('hex'));
  return value;
}

export default {
  format
}